query Reviews($id: Int!, $kind: ReviewKind!) {
  reviews(id: $id, kind: $kind) {
    id
    content
    writer {
      id
      firstName
      lastName
    }
    target {
      id
      firstName
      lastName
    }
  }
}
